# Zitadel (ARCHITECTURE.md L6, decision #10): the humans-and-apps IdP.
# TLS terminates at the edge; the API speaks h2c behind it (appProtocol).
# First login: zitadel-admin@zitadel.zitadel.cloudlab.kerbaras.com /
# Password1! — forced change on first login.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zitadel
  namespace: flux-system
spec:
  interval: 30m
  timeout: 15m
  targetNamespace: zitadel
  install:
    remediation:
      retries: 3 # first install races the CNPG cluster coming up
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: zitadel
      version: 10.0.4
      sourceRef:
        kind: HelmRepository
        name: zitadel
        namespace: flux-system
  values:
    zitadel:
      masterkeySecretName: zitadel-masterkey
      configmapConfig:
        ExternalDomain: zitadel.cloudlab.kerbaras.com
        ExternalPort: 443
        ExternalSecure: true
        TLS:
          Enabled: false
    # DSN mode (chart-recommended): the CNPG-generated app secret carries the
    # full connection URI — no password duplication, no DB superuser needed
    # (the zitadel user owns its pre-provisioned database). NOTE: top-level
    # `env` — only this propagates into the chart's init/setup jobs.
    env:
      - name: ZITADEL_DATABASE_POSTGRES_DSN
        valueFrom:
          secretKeyRef:
            name: zitadel-db-app
            key: uri
    service:
      appProtocol: kubernetes.io/h2c
    replicaCount: 1
