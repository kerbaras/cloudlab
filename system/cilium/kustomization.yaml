# Flux applies only what is listed here. values.yaml is the bootstrap Helm
# layer (README Stage 3) and deliberately NOT a resource.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - lb-pools.yaml
