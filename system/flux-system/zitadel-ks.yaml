apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: zitadel
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 2m
  timeout: 15m
  path: ./apps/zitadel
  prune: true
  wait: true
  dependsOn:
    - name: cloudnative-pg # Cluster CRD + operator
    - name: edge
  decryption:
    provider: sops # KMS via kustomize-controller's web identity (no secretRef)
  sourceRef:
    kind: GitRepository
    name: flux-system
