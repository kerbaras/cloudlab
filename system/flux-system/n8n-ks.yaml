apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: n8n
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 2m
  path: ./apps/n8n
  prune: true
  wait: true
  dependsOn:
    - name: cloudnative-pg # Cluster CRD + operator
    - name: openebs # binary-data PVC on lvm-thin
    - name: edge
    - name: external-secrets-config # secrets arrive from bao via ESO
  sourceRef:
    kind: GitRepository
    name: flux-system
