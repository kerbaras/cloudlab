# Cluster API (L3): the fleet stamper. No per-namespace policies dir — the
# five capi namespaces ride the clusterwide baseline CCNP backstop
# (system/cilium/clusterwide-baseline.yaml).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - source.yaml
  - helmrelease.yaml
