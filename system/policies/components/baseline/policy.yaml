# Tier: baseline (ARCHITECTURE.md §6.3) — deny-world ingress, east-west open.
# Day-one default for every namespace.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: baseline-ingress
spec:
  endpointSelector: {}
  ingress:
    - fromEntities:
        - cluster
